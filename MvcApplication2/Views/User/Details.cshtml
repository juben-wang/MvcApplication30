@model MvcApplication2.Models.UserDetailViewModel
<h2>Details</h2>
<fieldset>
    <legend>User</legend>
    <div class="display-label">
         @Html.DisplayNameFor(model => model.User.Name)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.User.Name)
    </div>

    <br/>
    <div class="display-label">
         西元新增時間
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.User.Create_Date)
    </div>

    <br/>
    <div class="display-label">
         民國新增時間
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.User.Create_Date,"TaiwanDate")
    </div>
    <br/>

    <div class="display-label">
         Email
    </div>
    <div class="display-field">
        @foreach (var data in Model.EmailList)
        { 
            @(data.Value)<br/>
        }
    </div>
   
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.User.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
